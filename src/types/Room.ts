export interface RoomFilter {
  hotel_id: number,
  nama_hotel: string,
  room_type_id: number,
  room_id: number,
  room_number: string,
  max_occupancy: number,
  cabang_hotel: string,
  room_status: string,
  price_per_night: number,
}
